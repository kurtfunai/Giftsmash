<h2>Are you sure you want to delete this list?</h2>
<h3>"<%= @list.title %>"</h3>
<%= link_to raw('<i class="icon icon-trash"></i> Yes, Delete'), @list, method: :delete, :class=>'btn btn-danger' %>
<%= link_to raw('No, Cancel'), @list, :title=>'View', :class=>'btn' %>