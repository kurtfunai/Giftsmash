$('#modal-window').modal('hide');
$("#item_<%= escape_javascript(@item.id.to_s) %>").remove()
$('#item-bin').prepend(
    '<%= j render("div_item") %>'
).isotope( 'reloadItems' ).isotope({ sortBy: 'original-order' });

$('#item-bin').trigger('itemAdded',['<%= @item.id %>','<%= @item.asin if @item.asin.present? %>']);